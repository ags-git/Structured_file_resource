CREATE TABLE check_name_rule_log (
    check_name_rule_log_s   INTEGER GENERATED BY DEFAULT AS IDENTITY,
    actual_name             VARCHAR(255),
    rule_type               rule_type,
    literal_value           VARCHAR(255),
    pattern_value           VARCHAR(255),
    dictionary_s            INTEGER,
    is_date                 BOOLEAN DEFAULT FALSE,
    date_sign               CHAR(1) DEFAULT NULL
);
